using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Animations;


public class MainWeapon : WeaponParent
{
    /*
    void Start()
    {
        //Set ScriptObj values
        SetValuesEvent();
        //muzzleFlash = scriptObj.muzzleFlash;
        //muzzleFlash = scriptObj.muzzleFlash;
        //muzzleFlash = Instantiate(muzzleFlash, transform.position, transform.rotation);

    }

    void OnEnable()
    {
        fpsCam.SetValuesEvent();
        SetValuesEvent();
        reloading = false;
    }

    //Functions
    public void SetValuesEvent()
    {
        spawner = gameObject.GetComponent<WeaponSpawner>();
        fpsCam = gameObject.GetComponent<FirstPersonCam>();
        recoil = gameObject.GetComponent<WeaponRecoil>();
        
        animator = gameObject.GetComponentInParent<Animator>();

        damage = scriptObj.damageValue;
        range = scriptObj.rangeValue;
        spread = scriptObj.spreadValue;
        maxAmmo = scriptObj.maxAmmoValue;
        currentAmmo = maxAmmo;
        fireRate = scriptObj.fireRateValue;
    }
    */
}
